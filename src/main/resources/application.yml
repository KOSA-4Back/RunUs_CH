spring:
  application:
    name: chat



  config:
    import: application-db.yml

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 8081


logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG